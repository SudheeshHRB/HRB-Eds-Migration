# HRB Nav & Footer — AEM Content Package (combined, deprecated)

Prefer the **separate** packages:

- Header: `packages/hrb-nav-content-1.0.0.zip` → `/content/NewHRBEds/nav`
- Footer: `packages/hrb-footer-content-1.0.0.zip` → `/content/NewHRBEds/footer`

See [packages/README.md](../README.md) for the CRXDE tree.

This combined package still seeds both pages under `/content/hrb-eds-migration/` (older site name). Use it only if your site root is still `hrb-eds-migration`.
